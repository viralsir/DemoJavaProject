DATABASE SYSTEM


PROGRAM  ------>       RDBMS (ORACLE ,POSTGRES,MYSQL)
          SQL

1) JDBC : JAVA DATABASE CONNECTIVITY
    1)        DRIVER     ADD /
    2)    BUFFER /LIB   SQL
         2.1 )  STATEMENT,   PREPARED STATEMENT ( BUFFER) (INSERT,UPDATE,DELTEE)
         2.2 ) RESULTSET   (SELECT)

    3) CLOSE THE CONNECTION


2) JPA  OR HIBERNET : JAVA PERSISTAN API
      ORM   (OBJECT RELATIONAL MAPPING )
      CLASS    --> TABLE
      MEMBER VARIABLE --> COLUMN
      OBJECT ---> ROW

      ANNOTATION -> @ENTITY
                    @PK ,@NOT NULL



3) SPRING JPA :-

